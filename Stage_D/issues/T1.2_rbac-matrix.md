---
name: Feature Task
about: PRD #19 기반 개발 태스크
title: "[Sprint 1] T1.2: RBAC 매트릭스 (lib/permissions.ts — 6 Role × 권한)"
labels: 'priority:must, area:backend, sprint:1, persona:admin'
---

## 🎯 Summary
- **Task ID**: T1.2
- **기능명**: 역할 기반 접근 제어 (RBAC)
- **목적**: 페르소나 5종 + Admin = 6 Role의 권한을 코드에서 일관 적용
- **MoSCoW**: Must
- **예상 시간**: 4h

## 🔗 References
- PRD: §5.2 RBAC, 부록 D 권한 매트릭스 표
- §3 AC MR-1-F1 (권한 없는 라인 접근 → 403)
- ADR: D18

## ✅ Task Breakdown
- [ ] `lib/permissions.ts` Permission enum 정의 (master·order·molding·extrusion·audit·admin × read·write·confirm 등)
- [ ] `ROLE_PERMISSIONS: Record<UserRole, Permission[]>` 매트릭스 (PRD 부록 D 그대로)
- [ ] `hasPermission(user, permission): boolean` 헬퍼
- [ ] `requirePermission(user, permission)` (Server Action용 — 권한 없으면 throw)
- [ ] Admin은 wildcard `'*'` 처리
- [ ] Vitest 단위 테스트 (각 Role × Permission 매트릭스)

## 🧪 Acceptance Criteria
### AC T1.2-1 (정상)
- **Given**: 박철수 (MOLDING_LEADER)
- **When**: `hasPermission(user, 'molding:write')`
- **Then**: `true` 반환

### AC T1.2-2 (정상 — 권한 없음)
- **Given**: 박철수 (MOLDING_LEADER)
- **When**: `hasPermission(user, 'extrusion:write')`
- **Then**: `false` 반환

### 🔥 AC T1.2-F1 (실패 — 잘못된 권한 ID)
- **Given**: 미정의 Permission 문자열 (예: `'foo:bar'`)
- **When**: `hasPermission(user, 'foo:bar')`
- **Then**: TypeScript 컴파일 에러 (Permission union type 활용)

## ⚙️ Constraints
- **타입 안전**: Permission은 union literal type
- **순수 함수**: 외부 의존성 없음 (테스트 용이)

## 🏁 DoD
- [ ] AC 모두 충족
- [ ] Vitest 단위 테스트 (6 Role × 모든 Permission)
- [ ] tsc 통과
- [ ] 매트릭스가 PRD 부록 D와 100% 일치 검증

## 🚧 Dependencies
- **Depends on**: T1.1
- **Blocks**: T1.3 (middleware), 모든 Server Action
