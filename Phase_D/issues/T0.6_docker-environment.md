---
name: Feature Task
about: PRD #19 기반 개발 태스크
title: "[Sprint 0] T0.6: Docker + docker-compose 환경 (사내망 배포 대비)"
labels: 'priority:must, area:infra, sprint:0, persona:admin'
---

## 🎯 Summary
- **Task ID**: T0.6
- **기능명**: Docker 컨테이너화 + docker-compose 통합
- **목적**: Cloud-Ready (12-Factor) 준수, 사내망 단일 서버 배포 + 미래 클라우드 이전 가능 구조
- **MoSCoW**: Must
- **예상 시간**: 4h

## 🔗 References
- PRD: §13 인프라, §5.5 모니터링, 부록 H Cloud-Ready
- WBS: Sprint 0
- ADR: D8 (사내망), D20 (Cloud-Ready 12-Factor)

## ✅ Task Breakdown
- [ ] `Dockerfile` 작성 (멀티스테이지: deps → builder → runner, Node 20 alpine)
- [ ] `.dockerignore` (node_modules, .next, .env, .git 등)
- [ ] `docker-compose.yml` (app + postgres + minio placeholder)
- [ ] `docker-compose.dev.yml` (개발용 — SQLite 유지)
- [ ] `next.config.js` `output: 'standalone'` 설정
- [ ] README에 실행 방법 (dev: `npm run dev`, prod: `docker-compose up`)
- [ ] 헬스체크 엔드포인트 `/api/health` (간단 200 OK)

## 🧪 Acceptance Criteria
### AC T0.6-1 (정상)
- **Given**: Dockerfile + docker-compose.yml 작성
- **When**: `docker-compose up --build`
- **Then**: app 컨테이너 정상 기동, `:3000` 접근 가능, 헬스체크 200

### AC T0.6-2 (정상)
- **Given**: Docker 이미지 빌드
- **When**: `docker images` 확인
- **Then**: 이미지 크기 < 500MB (멀티스테이지 효과)

### 🔥 AC T0.6-F1 (실패 — 환경변수 미주입)
- **Given**: docker-compose 환경변수 누락
- **When**: 컨테이너 시작
- **Then**: 시작 실패 + 명확한 환경변수 부족 메시지

## ⚙️ Constraints
- **12-Factor**: 모든 설정 환경변수, stdout 로그
- **이미지**: alpine 기반 (사내망 보안 정책 친화)
- **사용 금지**: Vercel-specific 빌드 옵션

## 🏁 DoD
- [ ] AC 모두 충족
- [ ] 멀티스테이지 빌드 (3 stage)
- [ ] 헬스체크 동작
- [ ] README 업데이트
- [ ] CI에서 `docker build` 통과 (Sprint 11 자동화)

## 🚧 Dependencies
- **Depends on**: T0.1
- **Blocks**: T11.7 (사내 배포)
